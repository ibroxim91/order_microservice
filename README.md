# Orders-сервис, реализованный на FastAPI.  

 Проект поддерживает регистрацию и авторизацию пользователей, создание и обработку заказов, асинхронное взаимодействие через RabbitMQ и Celery, а также кэширование данных в Redis.  

## Основные технологии:
### - FastAPI + Pydantic для API и валидации данных
### - PostgreSQL + SQLAlchemy + Alembic для работы с БД и миграций
### - RabbitMQ для обмена событиями 
### - Celery для асинхронной обработки задач
### - Redis для кэширования и брокера Celery
### - Docker Compose для развёртывания инфраструктуры
### - SwaggerUI для документации API

## 🚀 Установка и запуск 
### 1. Клонировать репозиторий 
```bash 

git clone git@github.com:ibroxim91/order_microservice.git

cd order_microservice
```

### 2.Запустить инфраструктуру через Docker Compose

``` bash

docker-compose up --build
 ``` 

### 3.Применить миграции Alembic

``` bash

docker-compose exec order_service alembic upgrade head

 ``` 

 ## SwaggerUI

 ``` bash
 http://localhost:8001/custom-redoc    // redoc
 http://localhost:8001/docs            // swagger
 
 ```

 ### Структура проекта

 ``` bash 

 app/
 ├── models/        # SQLAlchemy модели
 ├── schemas/       # Pydantic схемы
 ├── crud/          # Логика работы с БД
 ├── routes/        # FastAPI роутеры
 ├── auth/          # Авторизация и токены
 ├── utils/         # Кэш и вспомогательные функции
 ├── queue/         # RabbitMQ producer/consumer
 └── services/      # Сервисы
```